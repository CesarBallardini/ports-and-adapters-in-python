"""Driven adapters: the ways out of the application.

Every port here has at least two implementations, because a port with one implementation
has never been tested as an abstraction. All of them are verified against one shared
contract test suite (ADR-0014).
"""
