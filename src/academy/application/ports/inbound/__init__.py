"""Driving ports: what the outside world may ask the application to do.

Grouped by actor intent rather than one interface per use case, so a driving adapter can
depend on exactly the surface its actor needs and cannot reach past it.
"""
